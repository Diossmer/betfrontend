/*function handleSubmit(e) {
    e.preventDefault();
    onclick(sport);
  }

  function handleInputChange(e) {
    e.preventDefault();
    setSport({ ...sport, [e.target.name]: e.target.value });
  }
  FORMEXERCISES
  */